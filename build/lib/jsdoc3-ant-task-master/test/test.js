/**
 * Test that the jsdoc3 ant task runs (not that the jsdoc3 itself works)
 *
 * @constructor
 */

/**
 * sdfasdfasdf
 * @param foo {Array}
 * @returns {Array}
 * @constructor
 */

function Test(foo){
    return [];
}

Test.prototype.aTestMethod = function(){
}